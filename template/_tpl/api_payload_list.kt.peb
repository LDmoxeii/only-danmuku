package {{ basePackage }}{{ templatePackage }}{{ package }}

{{ use("org.mapstruct.Mapper") -}}
{{ use("org.mapstruct.factory.Mappers") -}}
{{ imports() }}

/**
 * {{ Comment }}
 *
 * 该文件由 [cap4k-ddd-codegen-gradle-plugin] 生成
 * @author cap4k-ddd-codegen
 * @date {{ date }}
 */
object {{ Payload }} {
{% if requestFields|length > 0 %}
    data class Request(
{%- for field in requestFields %}
        val {{ field.name }}: {{ type(field.type) }}{% if field.defaultValue is defined %} = {{ field.defaultValue }}{% endif %}{% if not loop.last %},{% endif %}
{%- endfor %}
    )
{%- else %}
    class Request
{%- endif %}
{% if responseFields|length > 0 %}
    data class Item(
{%- for field in responseFields %}
        val {{ field.name }}: {{ type(field.type) }}{% if field.defaultValue is defined %} = {{ field.defaultValue }}{% endif %}{% if not loop.last %},{% endif %}
{%- endfor %}
    )
{%- else %}
    class Item
{%- endif %}

    @Mapper(componentModel = "default")
    interface Converter {
        companion object {
            val INSTANCE: Converter = Mappers.getMapper(Converter::class.java)
        }
    }
}
